package enemies;

// minion del piso 2
public class Whisperers extends Enemy{

    public Whisperers(String name, int floor, int maxHp, int attack, int defense, long reactionTimeMs, int soulsReward) {
        super(name, floor, maxHp, attack, defense, reactionTimeMs, soulsReward);
    }
}

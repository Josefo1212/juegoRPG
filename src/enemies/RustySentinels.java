package enemies;

// minion del piso 1 (Centinela Oxidado)
public class RustySentinels extends Enemy{

    public RustySentinels(String name, int floor, int maxHp, int attack, int defense, long reactionTimeMs, int soulsReward, int expReward) {
        super(name, floor, maxHp, attack, defense, reactionTimeMs, soulsReward, expReward);
    }
}
